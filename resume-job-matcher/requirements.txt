# Resume-Job Matcher Dependencies
# Install with: pip install -r requirements.txt

# ── Core ────────────────────────────────────────────
# Embedding model for converting text to vectors
sentence-transformers>=2.2.0

# HTTP requests to Endee REST API
requests>=2.28.0

# MessagePack for decoding Endee search responses
msgpack>=1.0.0

# ── Rich Terminal UI ────────────────────────────────
rich>=13.0.0

# ── Web Chat Interface ───────────────────────────────
fastapi>=0.115.0
uvicorn>=0.30.0
python-multipart>=0.0.9

# ── Document parsing (for web upload) ───────────────
PyPDF2>=3.0.0
python-docx>=1.0.0

# Core dependencies (installed automatically)
# torch (required by sentence-transformers)
# numpy
# transformers
